odoo.define('web.AgGridController', function (require) {
    "use strict";

    var core = require('web.core');
    var BasicController = require('web.BasicController');

    var AgGridController = BasicController.extend({
        renderPager: function ($node, options) {

        }
    });

    return AgGridController;
});